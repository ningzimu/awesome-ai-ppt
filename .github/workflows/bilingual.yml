name: Bilingual

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  bilingual:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check bilingual Markdown
        run: python3 scripts/check_bilingual.py

